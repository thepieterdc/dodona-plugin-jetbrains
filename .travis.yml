language: java

sudo: false

jdk:
    - openjdk8
    - openjdk11
    - openjdk12

install: true

script: ./gradlew assemble

before_cache:
    - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
    directories:
        - $HOME/.gradle/caches/
        - $HOME/.gradle/wrapper/
